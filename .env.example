# Stripe secret key (TEST mode) — set this in Vercel → Project → Settings → Environment Variables.
# Never commit the real key. Local dev falls back to a demo success page if this is unset.
STRIPE_SECRET_KEY=your-stripe-test-secret-key
